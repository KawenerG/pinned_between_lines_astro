---
import { getCollection } from "astro:content";
import { base } from "../utils/env";
import BaseLayout from "../layouts/BaseLayout.astro";
const reviews = await getCollection("reviews");
---
<BaseLayout title="Pinned Between Lines">
<h1>📚 我的书评站：Pinned Between Lines</h1>
<p>共收录 {reviews.length} 篇书评：</p>

<ul>
  {reviews.map(r => (
    <li>
      <a href={`${base}reviews/${r.slug}`}>{r.data.title}</a>
      <small>（{r.data.series ?? "未分类"}）</small>
    </li>
  ))}
</ul>

<hr />
<p><a href={`${base}reviews/`}>按系列浏览书评</a></p>
</BaseLayout>